{
  "routes": [
    {
      "method": "GET",
      "path": "/gifs",
      "handler": "gifs.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/gifs/count",
      "handler": "gifs.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/gifs/:id",
      "handler": "gifs.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/gifs",
      "handler": "gifs.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/gifs/:id",
      "handler": "gifs.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/gifs/:id",
      "handler": "gifs.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
